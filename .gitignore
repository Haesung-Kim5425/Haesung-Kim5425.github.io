_site
.bundle
.DS_store
.idea
.jekyll-cache
.jekyll-metadata
.ruby-version
.sass-cache
.tweet-cache
.venv
assets/libs/
assets/rendercv/rendercv_output/*.typ
al-folio-upgrade-report.md
node_modules/
output/playwright/
test-results/
vendor

# Python bytecode from bin/ helper scripts
__pycache__/
*.pyc

# ---------------------------------------------------------------------------
# This repository is PUBLIC (it is served at kim5425.github.io). Internal working
# notes and session instructions must not be published with it.
# ---------------------------------------------------------------------------
CLAUDE.md
AGENTS.md
.claude/
NOTES.md

# Note on _bibliography/papers.bib: it IS committed, even though it is generated by
# bin/sync-sot.ps1. The GitHub Actions build only checks out this repository, so the
# file has to be present here for the publications page to render. Never hand-edit it.
